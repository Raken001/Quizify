{
  "name": "quizify-monorepo",
  "private": true,
  "scripts": {
    "build": "cd quizify-frontend && npm ci && npm run build",
    "build:frontend": "cd quizify-frontend && npm ci && npm run build",
    "start": "cd quizify-frontend && npm run start",
    "vercel-build": "cd quizify-frontend && npm ci && npm run build"
  }
}
