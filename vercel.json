{
  "builds": [
    {
      "src": "quizify-frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/quizify-frontend" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
